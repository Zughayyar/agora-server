# Copy this file to .env in your project root
# Application Configuration
APP_ENV=development
APP_PORT=3000
APP_VERSION=1.0.0

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agora_db
DB_USER=agora_user
DB_PASSWORD=agora_password
DB_SSL_MODE=disable

# Database Connection Pool Configuration (Optional - defaults provided)
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME_MINUTES=15
DB_CONN_MAX_IDLE_TIME_MINUTES=5

# Database Query Logging (Optional - for debugging)
DB_LOG_QUERIES=false

# AWS Deployment Configuration (for CI/CD reference)
AWS_IP=44.204.87.201
AWS_USER=ubuntu

# Production/Docker Configuration Notes:
# - When using Docker Compose, the postgres service is accessible as 'postgres' hostname
# - The APP_PORT is set to 8080 in docker-compose.yml to match the port mapping
# - For production, change DB_SSL_MODE to 'require' or higher
# - Set APP_ENV=production for production deployments
# - AWS_IP and AWS_USER are also needed as GitHub Secrets for CI/CD deployment